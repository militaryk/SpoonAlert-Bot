node_modules
.vscode
.env
# Leading slash matters: an unanchored "config.json" also matches
# test/fixtures/config.json, which must be committed.
/config.json
/userConfigs.json
/afkAlerts.json
/afkTracking.json

# Quarantined state files and in-flight atomic writes
*.corrupt-*
*.json.tmp

!package.json
